# Lab 25 AI review log (`lab25-001`)

- Date: 8/10/26

- Prompt used:
```
Implement a Spring @Service class named CustomerService in package com.northstar.crm.service.
  It constructor-injects a CustomerRepository (interface already exists with methods:
  save, findByCustomerId, findAll, existsByCustomerId, deleteByCustomerId).
  It should have these methods:
- getRequired(String customerId): returns the Customer or throws CustomerNotFoundException if missing
- create(Customer customer): throws DuplicateCustomerException if the customerId already exists, otherwise saves
- updateStatus(String customerId, CustomerStatus next): rejects illegal transitions (PROSPECT can go to
  ACTIVE or CLOSED; ACTIVE can go to SUSPENDED or CLOSED; SUSPENDED can go to ACTIVE or CLOSED;
  CLOSED cannot transition anywhere) by throwing IllegalStatusTransitionException, otherwise updates and saves
- list(): returns all customers
This is a pure business-logic layer. No Spring Web, no HTTP types, no JPA annotations.
```
- Suggestion summary: Copilot produced a `CustomerService` class with constructor
  injection and the four requested methods.
- Accepted: getRequired and list were correctly generated. Accepted.
- Rejected: imported org.springframework.http, rejected. Next run, `create` called `save()` without checking `existsByCustomerId`. Manually fixed.
- Verification: final `CustomerService.java` still has zero Spring Web/JPA imports, and methods match the reference shape.