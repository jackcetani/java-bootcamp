# Example inventory — replace hosts; never commit production IPs with secrets.
all:
  children:
    crm:
      hosts:
        crm-dev-01:
          ansible_host: 127.0.0.1
          ansible_user: crm-lab
          ansible_become_method: sudo
      vars:
        environment: dev